<p>Si desactiva el periodo de adjudicaci&oacute;n, se recomienda:</p>
<table class="aviso" style="text-align:left">
  <tr>
    <td class="doscientoscincuenta" style="font-size:0.5cm">Guardar asignaciones en historico</td>

        <td><%= link_to image_tag("boton2.png", :border=>0,:align=>"left"),"#",id:"enlaceGrabarHistorico"  %></td>
  </tr>
  <tr>
    <td class="doscientoscincuenta" style="font-size:0.5cm">Enviar correo de cierre a usuarios</td>

        <td><%= link_to image_tag("boton2.png", :border=>0,:align=>"left"),"#",id:"enlaceCierre"  %></td>
  </tr>
</table>
<p style="font-size:2em;"><%=  @mensaje %></p>

<script type="text/javascript">

$(function()
  {
    $("#enlaceCierre").click(function()
    {
      
      var mensaje="Se enviará un correo a todos los usuarios de la base, en el que se anunciará el cierre del periodo de adjudicación";
      if (confirm(mensaje)){
        var url="/periodos/enviar_correo_activo_off";
        $.ajax
        ({
          type:"POST",
          url:url
        });
      }
    });


     $("#enlaceGrabarHistorico").click(function()
    {
      
      var mensaje="Se guardarán las asignaciones de este periodo que cubran el periodo completo en un archivo histórico para su análisis estadístico";
      if (confirm(mensaje)){
        var url="/periodos/grabar_historico";
        $.ajax
        ({
          type:"POST",
          url:url
        });
      }
    });
  });
</script>