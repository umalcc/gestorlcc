
<div id="central" >
  <p>GESTOR DE RECURSOS<br>
     <%= image_tag ("pointer.gif") %>Asignaci&oacute;n personal de laboratorios en periodo de ex&aacute;menes
  </p>
  <div id="recuadro">  
    <p><%= image_tag ("bg_separ.gif") %></p>
       <% activo=Periodo.where("activo = ? and tipo= ?","t","Examenes").to_a %>
       <% nasig=Solicitudlabexa.where("usuario_id = ?",@usuario_actual.id).to_a.size %>
       <% if activo.size>0 and nasig>0 %>
           <div id="cuadrante2"><%= render :partial => "cuadrante2" %></div> 
       <% else %>
            <div class="aviso">
               <p>No tiene ninguna asignaci&oacute;n vigente de periodo de ex&aacute;menes</p>
               <p>Para cualquier consulta: tecnicos@lcc.uma.es</p>
            </div>  
        <% end %>  
  </div>
</div>

